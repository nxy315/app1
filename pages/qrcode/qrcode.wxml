<!--pages/qrcode/qrcode.wxml-->
<view id="qrcodeWrap">
  <canvas wx:if="{{!url}}" id="myCanvas" canvas-id="myCanvas"></canvas>
  <image wx:if="{{url}}" id="qrcode" src="{{url}}" bindtap="previewImg"></image> 
   <view wx:if="{{!url}}" id="save" bindtap="getImage">保存图片</view>   
</view>
